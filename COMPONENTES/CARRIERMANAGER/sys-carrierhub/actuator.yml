info:
  name: actuator
  type: http
  seq: 4

http:
  method: GET
  url: "{{gcp-ext-dev}}/sys-carrierhub/actuator/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
