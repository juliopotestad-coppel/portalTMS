info:
  name: getSellers
  type: http
  seq: 1

http:
  method: GET
  url: "{{spring}}/sys-carriermanager/sellers/2252"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
