info:
  name: actuator
  type: http
  seq: 12

http:
  method: GET
  url: "{{gcp-dev}}/sys-carriermanager/actuator/health"
  headers:
    - name: Host
      value: tenablewasIMkMl0yx0D.com
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
