info:
  name: getCarriers
  type: http
  seq: 2

http:
  method: GET
  url: "{{gcp-prod}}/sys-carriermanager/carriers"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
