info:
  name: credentials
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8080/dev/sys-carriermanager/credentials/4
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
