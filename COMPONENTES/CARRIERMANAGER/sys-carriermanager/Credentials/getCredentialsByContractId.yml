info:
  name: getCredentialsByContractId
  type: http
  seq: 1

http:
  method: GET
  url: "{{gcp-mac}}/sys-carriermanager/credentials/6"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
