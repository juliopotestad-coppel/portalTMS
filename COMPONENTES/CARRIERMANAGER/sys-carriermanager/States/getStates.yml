info:
  name: getStates
  type: http
  seq: 1

http:
  method: GET
  url: "{{spring}}/sys-carriermanager/states"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
