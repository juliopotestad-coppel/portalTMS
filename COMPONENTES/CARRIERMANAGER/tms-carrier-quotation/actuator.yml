info:
  name: actuator
  type: http
  seq: 3

http:
  method: GET
  url: "{{gcp-dev}}/tms-carrier-quotation/actuator/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
