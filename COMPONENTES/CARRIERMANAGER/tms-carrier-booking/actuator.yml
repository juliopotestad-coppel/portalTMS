info:
  name: actuator
  type: http
  seq: 2

http:
  method: GET
  url: "{{gcp-dev}}/tms-carrier-booking/actuator/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
