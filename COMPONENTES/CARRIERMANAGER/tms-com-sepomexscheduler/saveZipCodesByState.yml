info:
  name: saveZipCodesByState
  type: http
  seq: 1

http:
  method: POST
  url: "{{local}}/tms-com-sepomexscheduler/states/15"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
