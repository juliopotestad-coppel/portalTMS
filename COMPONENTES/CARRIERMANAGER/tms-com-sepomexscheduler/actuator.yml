info:
  name: actuator
  type: http
  seq: 2

http:
  method: GET
  url: "{{gcp-prod}}/tms-com-sepomexscheduler/actuator/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
