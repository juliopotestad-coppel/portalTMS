info:
  name: getCarrierCoppel
  type: http
  seq: 1

http:
  method: GET
  url: "{{gcp-qa}}/tms-proc-carrierselector/carriers-coppel"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
