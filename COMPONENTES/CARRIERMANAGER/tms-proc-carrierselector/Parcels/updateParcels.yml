info:
  name: updateParcels
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{gcp-dev}}/tms-proc-carrierselector/carriers/3/parcel/PRUEBA020524"
  body:
    type: json
    data: "{\r

      \  \"newParcelId\": \"\",\r

      \  \"status\": \"\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
