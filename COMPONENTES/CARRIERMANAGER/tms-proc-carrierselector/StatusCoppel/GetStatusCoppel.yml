info:
  name: GetStatusCoppel
  type: http
  seq: 1

http:
  method: GET
  url: "{{gcp-ext-dev}}/tms-proc-carrierselector/coppel/status"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
