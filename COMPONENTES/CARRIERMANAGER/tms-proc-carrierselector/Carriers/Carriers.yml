info:
  name: Carriers
  type: http
  seq: 1

http:
  method: GET
  url: "{{gcp-qa}}/tms-proc-carrierselector/carriers"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
