info:
  name: getVehicleConfigAll
  type: http
  seq: 2

http:
  method: GET
  url: "{{gcp-dev}}/tms-cap-fleetmanagement/vehicle-configurations"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
