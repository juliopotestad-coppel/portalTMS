info:
  name: getVehiclesTypes
  type: http
  seq: 4

http:
  method: GET
  url: "{{gcp-dev}}/tms-cap-fleetmanagement/vehicles/types"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
