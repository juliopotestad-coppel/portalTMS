info:
  name: getLicenseTypes
  type: http
  seq: 7

http:
  method: GET
  url: "{{gcp-qa}}/tms-cap-fleetmanagement/drivers/license-types"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
