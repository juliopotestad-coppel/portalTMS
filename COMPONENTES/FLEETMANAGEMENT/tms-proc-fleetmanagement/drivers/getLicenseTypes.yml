info:
  name: getLicenseTypes
  type: http
  seq: 6

http:
  method: GET
  url: "{{gcp-dev}}/tms-proc-fleetmanagement/drivers/license-types"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
