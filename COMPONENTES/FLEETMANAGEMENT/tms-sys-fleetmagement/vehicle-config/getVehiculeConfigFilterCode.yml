info:
  name: getVehiculeConfigFilterCode
  type: http
  seq: 1

http:
  method: GET
  url: "{{spring}}/tms-sys-fleetmanagement/vehicle-configurations?isArticulated=true"
  params:
    - name: code
      value: B2
      type: query
      disabled: true
    - name: isArticulated
      value: "true"
      type: query
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
