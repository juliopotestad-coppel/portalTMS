info:
  name: getConteinersTypes
  type: http
  seq: 5

http:
  method: GET
  url: "{{spring}}/tms-sys-fleetmanagement/containers/types"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
