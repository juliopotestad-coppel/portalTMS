info:
  name: actuator
  type: http
  seq: 8

http:
  method: GET
  url: "{{apigee-dev-mac}}/tms-sys-fleetmanagement/actuator/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
