info:
  name: getDistributionCenterById
  type: http
  seq: 3

http:
  method: GET
  url: "{{gcp-ext-dev}}/tms-sys-businessunits/distribution-centers/30001"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
