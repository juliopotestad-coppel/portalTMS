info:
  name: getDistributionCenter
  type: http
  seq: 2

http:
  method: GET
  url: "{{gcp-dev}}/tms-sys-businessunits/distribution-centers?has-aec=true"
  params:
    - name: has-aec
      value: "true"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
