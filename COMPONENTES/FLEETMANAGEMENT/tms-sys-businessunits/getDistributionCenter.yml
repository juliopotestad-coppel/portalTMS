info:
  name: getDistributionCenter
  type: http
  seq: 2

http:
  method: GET
  url: "{{local}}8081/dev/tms-sys-businessunits/distribution-centers?has-aec=true"
  params:
    - name: has-aec
      value: "true"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
