info:
  name: getBusinessUnits
  type: http
  seq: 1

http:
  method: GET
  url: "{{gcp-ext-dev}}/tms-sys-businessunits/business-units"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
