info:
  name: get_estados
  type: http
  seq: 1

http:
  method: GET
  url: https://api.copomex.com/query/get_estados?token={{token-copomex}}
  params:
    - name: token
      value: "{{token-copomex}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
