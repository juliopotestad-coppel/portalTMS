info:
  name: info_cp_geocoding
  type: http
  seq: 5

http:
  method: GET
  url: https://api.copomex.com/query/info_cp_geocoding/83295?type=simplified&token={{token-copomex}}
  params:
    - name: type
      value: simplified
      type: query
    - name: token
      value: "{{token-copomex}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
