info:
  name: get_municipios
  type: http
  seq: 2

http:
  method: GET
  url: https://api.copomex.com/query/get_municipio_por_estado/Nuevo León?token={{token-copomex}}
  params:
    - name: token
      value: "{{token-copomex}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
