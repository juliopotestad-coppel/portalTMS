info:
  name: get_cp_por_estado
  type: http
  seq: 3

http:
  method: GET
  url: https://api.copomex.com/query/get_cp_por_estado/Baja California?token={{token-copomex}}
  params:
    - name: token
      value: "{{token-copomex}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
