info:
  name: search_cp_advanced
  type: http
  seq: 4

http:
  method: GET
  url: https://api.copomex.com/query/search_cp_advanced/Baja California?token={{token-copomex}}
  params:
    - name: token
      value: "{{token-copomex}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
