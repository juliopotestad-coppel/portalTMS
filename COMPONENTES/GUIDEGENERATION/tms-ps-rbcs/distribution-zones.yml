info:
  name: distribution-zones
  type: http
  seq: 3

http:
  method: GET
  url: "{{spring}}/tms-ps-rbcs/distribution-zones/554/4"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
