info:
  name: liveness
  type: http
  seq: 3

http:
  method: GET
  url: "{{spring}}/bs-tms-guidegeneration/actuator/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
