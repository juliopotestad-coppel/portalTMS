info:
  name: carrier - DEV
  type: http
  seq: 3

http:
  method: GET
  url: "{{gcp-dev}}/tms-sys-routeconfiguration/distribution-business-unit/carrier?postalCode=43600"
  headers:
    - name: session.id
      value: "90395458"
  params:
    - name: postalCode
      value: "43600"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
