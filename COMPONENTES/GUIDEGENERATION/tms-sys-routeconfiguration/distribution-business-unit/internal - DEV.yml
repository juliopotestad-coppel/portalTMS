info:
  name: internal - DEV
  type: http
  seq: 1

http:
  method: GET
  url: "{{gcp-dev}}/tms-sys-routeconfiguration/distribution-business-unit/internal?postalCode=80000&settlement=10731&routeType=1"
  headers:
    - name: session.id
      value: "90395458"
  params:
    - name: postalCode
      value: "80000"
      type: query
    - name: settlement
      value: "10731"
      type: query
    - name: routeType
      value: "1"
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
