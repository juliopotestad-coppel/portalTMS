info:
  name: consultaDatosBodega
  type: http
  seq: 2

http:
  method: GET
  url: "{{spring}}/tms-sys-wscobroentregadomicilioapa/consultaDatosBodega?ciudad=4&tienda=800"
  params:
    - name: ciudad
      value: "4"
      type: query
    - name: tienda
      value: "800"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
