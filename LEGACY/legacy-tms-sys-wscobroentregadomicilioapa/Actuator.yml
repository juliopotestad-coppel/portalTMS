info:
  name: Actuator
  type: http
  seq: 1

http:
  method: GET
  url: "{{spring}}/tms-sys-wscobroentregadomicilioapa/actuator/info"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
