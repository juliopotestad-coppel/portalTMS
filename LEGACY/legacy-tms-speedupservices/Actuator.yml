info:
  name: Actuator
  type: http
  seq: 2

http:
  method: GET
  url: "{{local}}8081/dev/speedupservices/actuator/info"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
