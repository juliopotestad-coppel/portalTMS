info:
  name: Actuator
  type: http
  seq: 4

http:
  method: GET
  url: "{{spring}}/sys-speedupservices/actuator/info"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
