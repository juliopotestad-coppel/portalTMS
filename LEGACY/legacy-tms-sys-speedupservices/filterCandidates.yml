info:
  name: filterCandidates
  type: http
  seq: 5

http:
  method: POST
  url: "{{spring}}/sys-speedupservices/filterCandidates"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
