info:
  name: getRoutesToday
  type: http
  seq: 1

http:
  method: GET
  url: "{{spring}}/sys-speedupservices/routesToday"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
