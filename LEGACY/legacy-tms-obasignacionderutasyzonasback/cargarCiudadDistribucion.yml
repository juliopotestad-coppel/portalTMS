info:
  name: cargarCiudadDistribucion
  type: http
  seq: 12

http:
  method: POST
  url: "{{spring}}/obasignacionderutasyzonasback/cargarCiudadDistribucion"
  body:
    type: json
    data: '{ "bodega": 30001 }'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
